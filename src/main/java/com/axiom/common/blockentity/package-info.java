/**
 * Future home for block entities and their shared base classes.
 *
 * <p>The current codebase still uses {@code com.axiom.common.tile} as a
 * compatibility package. New code should prefer the blockentity naming
 * convention documented in the architecture note.</p>
 */
package com.axiom.common.blockentity;
